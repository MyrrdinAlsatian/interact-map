<section aria-labelledby="inventory-{{ pageSlug }}">
  <h2 class="page-title" id="inventory-{{ pageSlug }}">
    {{ pageTitle }}
  </h2>

  <div id="inventory-table" up-target="#inventory-table" tabindex="-1" aria-live="polite">
    @include('partials/inventory_rows')
  </div>
</section>
